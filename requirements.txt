# Core Dependencies
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
# `email-validator` is required because backend/models/user.py uses
# Pydantic's EmailStr type; without it test collection fails on
# `backend/models/user.py:17 class UserBase(BaseModel)` in CI.
email-validator>=2.0.0

# HTTP Client (async)
httpx>=0.26.0
requests>=2.31.0
python-dateutil>=2.8.0

# ML / Probabilistic Models
scipy>=1.11.0
xgboost>=2.0.0
lightgbm>=4.2.0
torch>=2.2.0

# Local Data Warehouse
duckdb>=0.10.0

# Config formats
pyyaml>=6.0

# Web Scraping
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
tqdm>=4.66.0
joblib>=1.3.0
python-dotenv>=1.0.0

# Caching (optional)
redis>=5.0.0
aioredis>=2.0.0

# Code Quality & Formatting
black>=24.0.0
flake8>=7.0.0
isort>=5.13.0
pre-commit>=3.6.0
mypy>=1.8.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Statistical challenger models — the floor the in-house engine must beat.
# Ships Dixon-Coles, bivariate/zero-inflated Poisson, Weibull copula and
# hierarchical Bayesian goal models, plus Elo/Massey/Colley/Pi ratings and
# RPS/Brier scoring. Measured 2026-08-08 on 12,289 walk-forward Wave A
# fixtures: Dixon-Coles .5977 Brier vs market .5770 (+.0207), against the
# in-house neural ensemble's +.0599. See backend/scripts/benchmark_dc_challenger.py.
penaltyblog>=1.11.0

# Optional but recommended
ipython>=8.0.0  # For interactive exploration
jupyter>=1.0.0  # For notebooks
